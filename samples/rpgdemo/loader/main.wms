export func main() {
    asset.preload(100);
    asset.preload(101);
    asset.preload(102);
    asset.preload(103);
    asset.preload(104);
    asset.preload(105);
    asset.preload(106);
    asset.preload(203);
    asset.preload(204);
    return "rpg-loader-ready";
}
