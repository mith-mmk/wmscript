import "chapter/part1.wms" as chapter;

export func main() {
    ext.message.clear();
    ext.message.log_clear();
    ext.message.show(
        "SplitImport",
        "Import graph was resolved by wmtoolchain.\nThis sample validates split-script compile flow."
    );
    recv();
    return "split import ok";
}
