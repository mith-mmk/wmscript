# Issue log

- `spec.md` is referenced by `AGENT.md`, but the repository currently uses `SPEC/*.md` as the source of truth and `spec.md` is absent in the working tree. Proceeding with the split spec files.

