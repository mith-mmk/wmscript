# WMScript v2 Standard Modules

The public source namespaces are `core`, `game`, `world`, `time`, `random`,
`input`, `scene`, `ui`, `audio`, `asset`, and `save`.

`ext.*` and string-keyed `state.*` are legacy-only and rejected by the v2
compiler. See `SPEC/hostapi.md` for module responsibilities.
